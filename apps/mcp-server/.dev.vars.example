# OZ Policy Builder — local dev environment vars
# Copy to .dev.vars and fill in values (never commit .dev.vars)

# No required vars for local dev — the MCP server runs without auth.
# For production deployment, add secrets via: wrangler secret put <NAME>
NODE_ENV=development
